- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vim: vim
    ~/.vimrc: vim/vimrc
    ~/.zshrc: zsh/zshrc
    ~/.zsh_plugins.txt: zsh/zsh_plugins.txt

- shell:
  - [git submodule update --init --recursive, Installing submodules]