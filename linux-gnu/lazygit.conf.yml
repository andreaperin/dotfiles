- defaults:
    link:
      relink: true

- clean: ['~/.config/lazygit/config.yml']

- link:
    ~/.config/lazygit/config.yml: lazygit/config.yml
     
- shell:
  - [git submodule update --init --recursive, Installing submodules]